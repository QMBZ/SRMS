<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qi.backend.mapper.RoleMapper">

    <!-- 结果映射 -->
    <resultMap id="RoleResultMap" type="com.qi.backend.entity.Role">
        <id column="role_id" property="roleId"/>
        <result column="role_name" property="roleName"/>
        <result column="role_desc" property="roleDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 获取所有角色 -->
    <select id="selectAllRoles" resultMap="RoleResultMap">
        select * from roles
    </select>

    <!-- 根据ID获取角色 -->
    <select id="selectRoleById" resultMap="RoleResultMap">
        select * from roles where role_id = #{roleId}
    </select>

    <!-- 新增角色 -->
    <insert id="insertRole">
        insert into roles(role_name, role_desc)
        values(#{roleName}, #{roleDesc})
    </insert>

    <!-- 修改角色 -->
    <update id="updateRole">
        update roles
        <set>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
            <if test="roleDesc != null">
                role_desc = #{roleDesc},
            </if>
        </set>
        where role_id = #{roleId}
    </update>

</mapper>